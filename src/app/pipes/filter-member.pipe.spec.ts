import { FilterMemberPipe } from './filter-member.pipe';

describe('FilterMemberPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMemberPipe();
    expect(pipe).toBeTruthy();
  });
});
