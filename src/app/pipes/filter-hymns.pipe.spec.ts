import { FilterHymnsPipe } from './filter-hymns.pipe';

describe('FilterHymnsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHymnsPipe();
    expect(pipe).toBeTruthy();
  });
});
